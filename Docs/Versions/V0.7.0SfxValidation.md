# v0.7.0 音效子项验收

日期：2026-09-18。Unity 2022.3.51f1 / URP 14，独立工作树 `sokoban-v070-sfx/Sokoban_3D_Test`，本地分支 `codex/v0.7.0-sfx`，基线 `06e61c1`。主工作区 v0.6.0 未提交改动、Blender 和第三方导入未纳入提交。

## 自动验证

- 首次 PlayMode 30 项：28 通过、2 失败。玩法与 UI 既有回归 22/22 通过；音效的 2 个失败分别为默认 Vorbis 压缩导致端点采样偏差，以及测试插槽误放低摩擦格（合法性验证正确拒绝）。
- 将 8 个 WAV 导入改成 PCM、预加载、关闭归一化；将供电测试改成普通地板上的连续两次推动。音效专项复测 **8/8** 通过。
- 追加作者试玩退出清理与真实 AudioListener 混音/主音量静音检查 **2/2** 通过。因此本次新增音效测试共 **10 项全部通过**，不是仅验证 AudioSource 的播放标记。
- EditMode 目录及构建前校验 **13/13** 通过，正式九关结构与当前参考解法有效。
- 运行素材生成器再次生成 8 个 WAV，SHA-256 全部与原文件一致；44.1 kHz 单声道、样本非静音、峰值及首尾包络检查通过。
- 编译完成后 Console 错误为零。测试明细（包括首次失败与修正复测）见 [结果记录](V0.7.0SfxTests.json)；原始 XML 在该工作树 `Library/SokobanTestResults/Run-*.xml`。

覆盖真实移动/推动、接触时间、整段滑行最终落位、通电/断电、延后完成、受阻限频、动作中撤销/重开/切关、暂停停止动作且 UI 可点击、恢复不重播接触、主菜单/关卡单监听器、作者试玩及销毁、静音时规则和计数不变。UI 既有回归覆盖设置保存与页面导航。BGM 与独立音量不属于本次提前子项。

## Windows 构建与 Player

- 显式以 `Assets/Scenes/Bootstrap.unity` 构建非开发 Windows x64 包：**Succeeded，0 errors，4 warnings，91,989,820 bytes**。构建结果：`Logs/sfx-build-result.txt`，产物：`Builds/V070Sfx/SokobanSfx.exe`。
- 4 条构建警告均是 MCP WebSocket 在构建过程中断连/保活失败；Editor 随后恢复连接。构建报告的 packedAssets 中确认包含全部 8 个音效 WAV。
- 使用 Windows computer-use 实际点击主菜单“开始游戏”→ L04 俯视关卡 → HUD 暂停 → 返回主菜单 → 退出，画面和转场正常，日志无音效缺失、监听器重复或运行时异常。
- 原生 `d` 键自动化单次注入后步数没有变化，故不将其计为独立 Player 移动验收；动作与声音同步以真实 Unity PlayMode 命令回归为证。Player 的主观试听/键盘实玩仍待用户体验确认。
- Player 日志含 URP fallback shader 警告（StencilDeferred / FallbackError / Lit），已观察关卡画面正常；本轮不改渲染管线。日志：`Logs/sfx-player-ui.log`。

## 边界

自动采样证明音频实际进入 Unity 混音且主音量静音有效，不能代替人耳对音色、响度舒适度的主观评审；本轮不宣称已经完成人工试听。素材来源和播放策略见 [音效说明](../Audio/Sfx.md)。v0.7.0 的 BGM、独立音量和图形反馈仍待后续，整版保持进行中。

验证工作树复制了主工作区已有的 `Assets/Plugins`（含 DOTween）作为本地依赖；这些既有第三方文件原本未纳入 Git，本次不把它们混入音效提交。干净机器仍须具备项目已有依赖。与 v0.6.0 的合并验证待后续，不将本分支通过等同于未提交 UI/存档组合后的验收。

## 合并 main 后复验（2026-09-18）

音效分支 `f556eef` 已推送，合并至含 v0.6.0 的 main（合并前 `e7c6fcd`）。7 处文件冲突保留双方需求；音效测试同步新版主菜单路径，并注入内存进度以免污染正式存档。PlayMode **39/39**、EditMode **23/23** 通过，编译及 Console 零错误；本次未重复独立 Player 构建/试听。

用户要求删除独立工作树：其中日志、原始测试 XML 和两份不同于主工作区的本地配置归档至主工作区 `Logs/V070SfxWorktreeArchive/`（Git 忽略），原独立构建产物随工作树清理。上文历史路径指原验收位置。当前合并原始 XML 位于主工作区 `Library/SokobanTestResults/`，摘要追加在 [测试记录](V0.7.0SfxTests.json)。文档本地链接检查只发现原工作区已删除的课程 PPT，不恢复用户删除内容。
