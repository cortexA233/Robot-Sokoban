# v0.2.1 · 删除原前三关

状态：受阻 · 类型：关卡内容维护 · 起始提交：`027c07b`。实现与定向检查完成；最后的整组 PlayMode 复测被取消，未标记完整验收通过。

## 目标与范围

- 删除原正式关卡 L01、L02、L03 的关卡 JSON、参考解法、对应 Unity `.meta` 和作者配方。
- 正式目录改为 L04 → L05 → L06，保留其 ID、布局、GUID 与参考解法；同步启动入口、编辑器示例与目录创建工具、旧关卡专属主题绑定。
- 更新受影响的现有测试与当前使用说明；历史报告与 GDD 中标明为历史的设计示例保留。
- 不修改玩法规则、LAB01 低摩擦测试关、引擎/包版本、美术资产或其他进行中版本；不推送或发布。

## 验收计划

1. 确认删除资源及 `.meta` 无残留，目录、启动资源键和编辑器入口没有悬空引用。
2. Unity 导入并编译通过，检查 Console 错误。
3. 运行受影响的 EditMode 测试（目录、作者工具、旧格式兼容与参考解法），验证 L04–L06 及 LAB01 解法仍有效。
4. 运行受影响的 PlayMode 测试，覆盖主菜单进入、三关选关、连续通关、最终结算及表现重建。
5. 检查文档链接、差异与无关工作保留，创建 `[v0.2.1]` 聚焦提交。

## 执行结果与限制

- 已删除 L01–L03 的 6 个 Unity 数据资源、6 个 `.meta` 和3份作者配方；目录与 Bootstrap 默认入口改为 L04 → L05 → L06。同步编辑器入口、目录创建工具、主题绑定、现有测试和当前说明。
- 静态检查通过：删除资源的6个 GUID 及旧关卡 ID 无运行时/编辑器引用；保留关卡、解法、配方与 `.meta` 内容未变；12份 JSON、69个文档本地链接及本轮差异检查通过。原作者/现场草稿文件未变。
- Unity 2022.3.51f1 已导入并编译，最终 Console 错误0条。EditMode 定向复测 **68/68 通过**，包含目录、作者操作、旧格式兼容、现场编辑与正式/实验关参考解法；MCP job `5253be8b7b0a41d7accf6c47076d3ddb`。
- 运行过 CampaignFlow、StationUI、StationKitGameplay、GM 的26个 PlayMode 用例。整组返回 `failed`，唯一报告失败是旧测试要求正式关卡覆盖 `FloorGrate`，当前紧凑地图没有该装饰样式。加入独立作者测试图保留全部11个资产的覆盖，修正后的用例 **1/1 通过**；MCP job `0059589a991a490684df4d516b779549`。不将失败组的进度计数表述为整组通过结果。
- 过程中编辑器撤销记录曾恢复旧目录，导致启动引用缺失；已清理本轮资源的旧撤销记录并使用不生成撤销的保存方式，重跑68项 EditMode 后实读目录仍为三关且没有待保存改动。
- 最后的整组 PlayMode 复测 job `addf591f8abd4ba2a3e7fa58206b1b66` 收到 `Cancelled by user` 后中断。确认 Unity 原生 EditMode/PlayMode 测试均已停止后，清理遗留的 MCP 运行标记与孤立 job；保留编辑模式，不继续启动被取消的测试。**剩余验收：在共享 Editor 空闲时完成这26项 PlayMode 的最终整组复测。**
- 本地证据在 `Logs/LevelRemovalV0.2.1/`。本轮未生成 Player 构建；不包含 v0.2.0 的并行视觉修改、已独立提交的 v0.3.0 策划、原有机器人/GM Prefab/插件与项目配置修改，不推送或发布。
