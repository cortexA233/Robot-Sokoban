# v0.3.0 · 转向板与紧凑运输关卡

状态：**已完成 · 已封版** · 类型：玩法 / 关卡 · 策划起始提交：`027c07b` · 实施起始提交：`43de549`。

## 目标与范围

- 按确认方案实现固定转向板，与供电门、低摩擦轨道组合，追加 L07–L12 六关。正式目录为 L04–L12，共九关。
- 新关尺寸依次为 7×7、7×7、8×8、8×9、9×9、9×9，包含外围墙，格长 1 m。原 L04–L06 的布局、ID、GUID 和证明哈希保留。
- 完成共享规则、schema v3 与 v1/v2 兼容、作者画笔/旋转/保存/草稿/撤销/现场编辑、转向板表现、正式配方与解法、关卡目录、文档和 Windows 构建。
- 转向是固定方向的运输动作，不增加运行时旋转、压力板、拉箱、连推或新的胜利条件。保留 Unity 2022.3.51f1、URP 14、Cinemachine 2.x 和 CoplayDev 10.2.0。
- **本版本范围已冻结。** 后续新增功能另立版本；本次没有创建 Git 标签、改 PlayerSettings 产品版本、推送或发布。完整 Take-home 尚待事项继续由 [实现状态](../ImplementationProgress.md) 单独维护。
- [关卡设计与实际格图](../Gameplay/RedirectorLevelPlan.md) 是关卡说明；[验收材料](V0.3.0Validation/README.md) 是本次实现证据。

## 验收计划

1. 共享规则覆盖四向进入/离开、连续转向与滑轨、受阻停留、两箱类型、门停靠与断电、循环整条拒绝、一次撤销、初始/GM 不自动启动。
2. 新旧数据与解法兼容，作者画笔、方向修改、复制、缩图、草稿恢复、真实窗口输入和现场应用/恢复贯通。
3. 六关通过制作链保存与正式规则回放；机关必要性独立分析；九关连续游玩、末关选关、结算和生命周期检查。
4. 真实画面检查最大地图、供电交接、被箱子覆盖的板方向与双视角；Windows 非开发构建与启动检查。
5. 检查文档、配方一致性、资源 GUID 和最终差异；保留无关工作，聚焦提交后封版。

## 执行结果与限制

- **EditMode 全组 122/122 通过**；最终规则与六关配方定向复测 **31/31 通过**。旧 v1/v2 数据的线格式、内容哈希与解法保持有效。
- **PlayMode 全组 46/46 通过**，包括九关完整动画回放、末关滚动选关、暂停、撤销/重开、镜头切换、GM 与资源释放；最后的箭头/门标签避让修复后，表现专项 **10/10 通过**，包含新增的初始与交接局面避让测试。
- 正式六关参考移动/推动为 L07 10/2、L08 5/3、L09 48/10、L10 45/11、L11 19/10、L12 41/12。不宣称最优、唯一解或独立玩家难度排名。
- 逐一固定普通箱、封闭各门、断开临时电源的离线变体均搜索耗尽无解；这些是关卡用途分析，独立于 Unity 正式回放。
- 五张最终实机截图已检查，覆盖 1080p/720p、9×9、供电交接、箱子压板及跟随/俯视。720p 交接局面中门标签与两块转向板的屏幕重叠数为 0。
- Windows x64 非开发构建成功，0 错误、0 警告，约 81.63 MB。入口：`Builds/V0.3.0/StationRestart.exe`。独立进程启动并初始化框架，检查 8 秒无异常；此启动检查不冒充独立 Player 内的九关交互验收。
- 首次 PlayMode 初始化超时后使用 120 秒初始化窗口恢复；一个新测试错误地在暂停菜单打开后发送切镜头指令，已按现有输入契约修正并通过完整复测。实机发现门标签遮挡新箭头，已增加板区域避让和邻近角落候选位置，并通过专项及截图复核。
- 完整 NUnit 结果树另存到 Library，防止域重载丢失 MCP 汇总；本轮原始 XML 随验收材料保存。最终 Console 无错误，Editor 回到干净的 Bootstrap 非运行状态，Game View 恢复原尺寸。
- 构建临时写入的预加载资产字段已还原；开始时已有的 Robot、GM Prefab 和 ProjectSettings 未提交差异与备份完全一致。第三方未提交导入保留。
- 版本封版仅覆盖本轮功能与六关；独立用户难度测试、进度存档、完整目录管理及整体 Take-home 交付仍见现有状态记录。未为这些既有后续项扩大本轮范围。
