# v0.3.2 · 后续迭代路线图与立项约定

状态：已完成 · 类型：文档 / 版本规划 · 起始提交：`cc2ee0e3e640d9be6b5914ca77a425e7a754cb47`。

## 目标与范围

- 汇总用户提出的 UI 文案与样式、关卡编辑器易用性、场景去文字与连接配色、第三人称镜头与场景美术四个迭代方向。
- 结合当前实现、设计约定和剩余交付范围，记录风险、建议补充项、依赖、立项前待定问题及可检查的验收目标。
- 新增统一路线图，并接入 Agent 指南、版本台账、文档索引、目录规范和实现状态入口。后续每次迭代仍须先登记正式版本计划，再实现、验收与提交。
- 本版本仅交付规划文档；路线图不代表后续功能已经立项、实现或验收。具体方案待定的条目使用稳定的路线图编号，不提前占用开发版本号。
- 不修改游戏脚本、关卡、场景、美术、引擎依赖或项目设置；保留原有未提交工作，不推送或发布。

## 验收计划

1. 四项用户方向均有明确条目，保留尚待讨论的范围，不替用户定死 UI 风格、分类、调色板、镜头参数或墙高。
2. 补充建议与现有缺口对应，不把已实现能力写成未实现，不把旧验收结果用作新方案证据。
3. 核对版本编号唯一、状态一致、路线图与正式版本计划边界清楚；检查本轮 Markdown 本地链接与 Git 差异。
4. 只暂存本版本文档，创建 `[v0.3.2]` 聚焦提交。文档变更不触发 Unity 测试或构建。

## 执行结果与限制

- 已按“已规划 → 进行中 → 已完成”执行；立项前最高已登记版本为 v0.3.1，本次采用下一修订号 v0.3.2。
- 已新增 [迭代路线图](Roadmap.md)：R01–R04 完整对应用户提出的四项方向；R05–R09 为目录管理、进度保存、引导与难度、基础反馈和完整交付验收建议。每项记录范围、依赖、待定问题和验收目标，列出建议顺序及可调整条件。
- 已明确颜色表达多对多供电关系的限制、颜色与图形冗余、目标/连接/状态分层，以及镜头参数、墙体视觉和镜头碰撞体联合检查。相机与墙体数值来自当前代码，只读核对不冒充实机问题诊断或新效果验证。
- 已将路线图接入 Agent 指南、版本台账、文档索引、目录规范和实现状态；所有后续独立迭代（含文档/配置）均先立版本计划。待定方向未预占开发版本，现有封版与受阻状态未改写。
- 本轮 7 份 Markdown 的本地链接与标题锚点检查通过；6 个版本登记无重号，9 个路线图条目的总表与详情对应一致；本轮差异检查通过。
- 本次仅修改上述规划与导航文档；原有机器人源文件、GM Prefab、项目设置和第三方导入未纳入提交。未操作 Unity、运行游戏测试或构建；美术效果、镜头手感和建议功能均留待各自正式立项后实现与验收。未推送或发布。
