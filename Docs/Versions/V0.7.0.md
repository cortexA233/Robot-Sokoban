# v0.7.0 · 简单音效、BGM 与操作反馈

状态：进行中 · 类型：音频 / 操作反馈 · 需求：R08 · 策划起始提交：`2a13adb` · 实施起始提交：`06e61c1`。

## 本次提前实施边界（2026-09-18）

用户要求先在新本地分支实现音效部分。分支 `codex/v0.7.0-sfx` 从 `06e61c1` 建立，使用独立工作树，主工作区未提交的 v0.6.0 和美术改动不纳入。本次交付八类原创音效、事件同步、限频、取消清理和现有主音量控制。BGM、音乐/音效独立音量及设置页控件、受阻/完成图形反馈仍按下述完整计划后续实施，v0.7.0 整版保持进行中。音效子项不依赖 v0.6.0 UI；合并时保留两版对 LevelRunner/UI 的改动。

## 目标与范围

- 排期前置：[v0.6.0](V0.6.0.md)。落实用户明确要求的简单关卡音效和简单 BGM，保持低干扰、小规模；共用设置页风格与现有动作、供电、导航状态。
- 基础音效覆盖移动/推动、受阻、落位、通电/断电、完成和 UI 点击。根据真实事件触发，限制重复受阻及密集机关变化的叠播，音乐或音效不驱动玩法逻辑。
- 首轮接入 **一首关卡共用、可循环的简单 BGM**。默认同曲切关和重开保持连续，暂停降低音乐音量并停止动作音效，暂停菜单点击音仍可用；恢复后回到用户音量，返回主菜单淡出，退出会话清理。作者试玩使用相同播放规则，退出试玩不遗留声音对象。
- 保留主音量，新增音乐与音效独立音量，零音量可静音；调整立即生效并在重启后恢复，兼容现有设置。新增控件由本版完整实现并沿用 v0.6.0 样式。
- 受阻与完成提供最小的简短图形反馈，复用现有角色锚点与相机朝向逻辑；音画反馈在撤销、重开、切关后不得延迟触发旧事件。
- 开工时细化音色、音乐风格、素材来源、循环接缝和默认混音。使用可随项目分发的现有或原创简单素材，记录来源与导入设置；正式构建需含音频资源及其 `.meta`，不能只在 Editor 有声。
- 不包含每关专属音乐、动态分层配乐、配音、大量特效或机器人动画重做。若改变默认曲目数或暂停/跨关播放规则，在本版本内明确更新计划。

## 验收计划

1. Unity 编译和 Console 检查；对动作取消、连续触发、暂停、重开、切关、会话销毁、设置兼容和保存运行针对性检查。
2. 在实际关卡与独立 Player 中试听全部音效；连续播放 BGM 至少两个完整循环，确认无明显接缝、爆音、叠播或缺失。记录素材、实际音量与试听环境，播放状态断言不代替试听。
3. 连续执行开始/重开/下一关/选关/返回菜单/再进入，确认只有预期的音乐实例，同曲跨关连续，返回菜单淡出；暂停、恢复和作者试玩退出符合约定。
4. 分别调整主音量、音乐和音效到零/中间值，重启独立 Player 检查保存；关闭音乐不影响音效，关闭音效不影响音乐，静音仍能理解稳定状态。
5. 检查长按受阻节流，以及推动/滑行动作中撤销、重开后无旧声音、旧气泡或重复完成反馈；双视角下图形反馈正常，规则和计数不变。
6. 检查音频资源引用、素材来源、构建打包、文档与差异，更新台账并创建 `[v0.7.0]` 聚焦提交。

## 执行结果与限制

- 本次音效代码与素材已交付：8 类原创 PCM 音效、真实表现时点触发、限频、取消/暂停/销毁清理、主菜单监听器及主音量控制。
- 新增音效 PlayMode 10/10；已有玩法/UI 回归 22/22；目录/构建 EditMode 13/13。Windows 非开发构建成功、全部音效打包、Player 主菜单→关卡→暂停→主菜单通过；编译后 Console 无错误。
- 主观试听与独立 Player 键盘动作验收尚未完成；BGM、独立音量与图形反馈继续留在本版本，整版保持进行中。首次失败、修复、工具/依赖限制见 [音效子项验收](V0.7.0SfxValidation.md)，素材来源见 [音效说明](../Audio/Sfx.md)。
- 详见 [路线图 R08](Roadmap.md)；初始计划由 [v0.3.3](V0.3.3.md) 整理。完成后接续 [v0.8.0](V0.8.0.md)。

### 合并到 main（2026-09-18）

用户要求先推送音效分支、再合并 main 并删除独立工作树。音效分支 `f556eef` 已推送至 `origin/codex/v0.7.0-sfx`；合并实施起点为 main `e7c6fcd`（v0.6.0 已完成）。保留双方进度/帮助/UI 简化与音频功能；同步音效测试的主菜单路径和内存进度夹具。合并后 PlayMode 39/39（音效/玩法/UI/进度）、EditMode 23/23（目录/构建守卫/进度）通过，Unity 编译及 Console 零错误；文档/JSON/差异检查通过，原有已删除 PPT 链接仍按既有状态保留。原工作区未提交文件不纳入合并提交。
